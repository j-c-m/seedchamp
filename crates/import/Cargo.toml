[package]
name = "seedchamp-import"
version = "1.0.0"
edition = "2021"
description = "Import/export rtorrent and Transmission session trees for seedchamp"
license = "MIT"

[lints]
workspace = true

[dependencies]
seedchamp-engine = { path = "../engine" }
hex = "0.4"

[dev-dependencies]
tempfile = "3"
